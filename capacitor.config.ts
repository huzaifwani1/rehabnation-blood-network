import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.rhfi.bloodnetwork',
  appName: 'rehabnation-blood-network',
  webDir: 'dist'
};

export default config;
